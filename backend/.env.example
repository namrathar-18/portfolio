# MongoDB Atlas — credentials passed separately so '@' in the password is safe
MONGO_URI=mongodb+srv://your-cluster.mongodb.net/portfolio
MONGO_USER=your_db_user
MONGO_PASS=your_db_password

PORT=5000
NODE_ENV=development
